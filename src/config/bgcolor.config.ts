export const DEFAULT_BG_COLOR_CONFIG = {
    // Input Bar
    search_bar: {
        light: "#ffffff",
        dark: "url('/assets/inputbar-gradient.webp')"
    },

    // Send Button
    send_button: {
        light: "url('/assets/inputbutton-gradient.webp')",
        dark: "linear-gradient(to top right, rgba(255, 255, 255, .35), rgba(0, 0, 0, .2))"
    },

    // Response Window
    response_window: {
        light: "rgba(255, 255, 255, .95)",
        dark: "rgba(0, 0, 0, .8)",
        container_light: "rgba(255, 255, 255, .98)"
    },

    // Hot Questions Floating Menu
    hot_questions: {
        light: "#ffffff",
        dark: "transparent",
        hover_light: "rgba(0, 0, 0, 0.05)",
        hover_dark: "rgba(255, 255, 255, 0.05)"
    },

    // Hot Questions Container
    hot_questions_container: {
        light: "#ffffff",
        dark: "transparent"
    },

    // Suggested Questions (Related Questions)
    suggested_questions: {
        hover_light: "rgba(0, 0, 0, 0.05)",
        hover_dark: "rgba(255, 255, 255, 0.05)"
    },

    // Cards (Related Content, etc.)
    cards: {
        light: "#ffffff",
        dark: "rgba(0, 0, 0, 0.8)"
    },

    // Response Related Content (Container)
    response_related_content: {
        light: "transparent",
        dark: "transparent"
    },

    // Interactable Elements (Buttons, Items)
    book_session_button: "rgb(0, 0, 0)",
    notification_badge: "#ef4444",

    hover_states: {
        related_question_light: "rgb(243, 244, 246)",
        related_question_dark: "rgba(255, 255, 255, 0.1)"
    }
};
