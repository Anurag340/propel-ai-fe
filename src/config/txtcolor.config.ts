export const DEFAULT_TXT_COLOR_CONFIG = {
    // Input Bar
    search_bar: {
        light: "#4b5563",
        dark: "#ffffff",
        placeholder_light: "rgba(75, 85, 99, .7)",
        placeholder_dark: "rgba(255, 255, 255, .5)" // Assumed consistent dark placeholder
    },

    // Response Window
    response_window: {
        light: "#374151",
        dark: "#f1f5f9",
        heading_light: "#1F2937", // Based on Hot Questions/Cards
        heading_dark: "#e5e7eb"
    },

    // Hot Questions / Lists
    hot_questions: {
        light: "#1F2937",
        dark: "rgb(229, 231, 235)"
    },

    // Content Cards (Related Content, etc.)
    cards: {
        light: "#282828",
        dark: "#ffffff"
    },

    // Buttons
    buttons: {
        send_light: "#ffffff", // Icon color, but text if present
        send_dark: "#ffffff",
        book_session: "#ffffff",
        secondary_light: "#4b5563",
        secondary_dark: "#ffffffd2"
    },

    // Loader
    loader: {
        light: "#6b7280", // Gray-500
        dark: "#9ca3af"   // Gray-400
    }
};
