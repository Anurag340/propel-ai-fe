export const SectionHeader = ({ children }: { children: React.ReactNode }) => (
    <h3 className="text-[10px] font-bold text-[#bbbbbb] uppercase tracking-wide mb-2 mt-4 select-none">
        {children}
    </h3>
);
